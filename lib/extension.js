/*exports.exists = function(name)
{
	return settings.extensions[name || 'default'];
};
exports.get = function(query)
{
	var matches = query.match(/\.([^\.]*)(\?|$)/);

	if(!matches)
	{
		return '';
	}
	return matches[1];
};*/